# Virtual env from host must not overwrite the one built in Docker
.venv
venv
.env
.env.*

# Git and IDE
.git
.gitignore
.cursor
*.md

# Python cache (already in .gitignore, keep for Docker)
__pycache__
*.py[oc]
build/
dist/
*.egg-info
